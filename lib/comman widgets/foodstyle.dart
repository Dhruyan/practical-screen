import 'package:flutter/material.dart';

class FoodStyle extends StatelessWidget {
  const FoodStyle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}
