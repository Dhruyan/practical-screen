import 'package:flutter/material.dart';

class FoodStyle extends StatelessWidget {
  const FoodStyle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return
  }
}
